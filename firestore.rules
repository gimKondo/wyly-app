service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write;
    }
    // posts by others
    match /{path=**}/users/{userId}/posts/{docId} {
      allow read;
    }
    // posts by self
    match /{path=**}/users/{userId}/posts/{docId} {
      allow read, write: if userId == request.auth.uid;
    }
    // timelines by self
    match /{path=**}/users/{userId}/timelines/{docId} {
      allow read: if userId == request.auth.uid;
    }
  }
}
